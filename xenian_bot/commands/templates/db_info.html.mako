<b>Tag: [${info['tag']}]</b><br>
<pre>Documents: ${info['document']}
    Videos: ${info['video']}
    Photos: ${info['photo']}
    Stickers: ${info['sticker']}
    Audios: ${info['audio']}
    Voices: ${info['voice']}
    Texts: ${info['text']}</pre>

<i>In Total:</i> <code>${info['total']}</code>
<br>
<br>
Keep in mind that in Telegram some videos are saved as documents and vis versa, so are voices as audios.
